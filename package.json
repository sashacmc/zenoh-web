{
  "name": "zenoh-web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "hugo && npx pagefind --site public",
    "dev": "hugo server"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.16",
    "pagefind": "^1.3.0",
    "tailwindcss": "^3.4.17"
  }
}
